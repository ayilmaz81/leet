#pragma once
#include <vector>
#include <string>

namespace PalindromePartitioning
{
    std::vector<std::vector<std::string>> Solve(const std::string& input);
    
} // namespace PalindromePartitioning
