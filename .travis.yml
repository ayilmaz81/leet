language: cpp

matrix:
  include:
    - os: linux
      dist: bionic
      compiler: gcc
      addons:
       apt:
         packages:
           - cmake-data
           - cmake
           - libgtest-dev

script:
     - mkdir -p build
     - cd build
     - cmake ../
     - make
     - ctest -V
